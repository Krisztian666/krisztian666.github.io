        <TextView//{speak:"Add TextView to the end."}
            android:id="@+id/urlTextView"
            android:textSize="12dp"
            android:text="URL"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_below="@id/dateTextView"
            />
    </RelativeLayout>
